*/  Write a program whose input is:firstName middleName lastName
and whose output is: lastName, firstInitial.middleInitial.
If the input has the form: firstName lastName.
the output is: lastName, firstInitial. zybooks 3.26 Lab(Name format) */

import java.util.Scanner; 

public class LabProgram {
   public static void main(String[] args) {
      Scanner scnr = new Scanner(System.in);
      String wholeName = scnr.nextLine();
      String [] parts = wholeName.split (" ");    //split at space
      
      String firstName = parts[0];
      String middleName;
      String lastName;
      
      if (parts.length == 3 ) {
         middleName = parts[1];
         lastName = parts[2];
         System.out.println(lastName + ", " + firstName.charAt(0) + "." +  middleName.charAt(0) + ".");
      }
      else if (parts.length == 2) {
         lastName = parts[1];
         System.out.println(lastName + ", " + firstName.charAt(0) + ".");
      }
      else {
         System.out.println("Invalid format");
      }
   }
}

// convert all first letters of each word to uppercase?
