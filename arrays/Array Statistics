Write a program to calculate the minimum, maximum, mean, median, mode, and whether an array is a palindrome.
    If input is:    
    9 1 2 2 2 3 3 4 5 6

    the output is:    
    Minimum: 1
    Maximum: 6
    Mean: 3.1
    Palindrome: false
    Median: 3.0
    Mode: 2

import java.util.Arrays;
import java.util.Scanner;
public class LabProgram {
   public static void main(String args[]) {
      Scanner scnr = new Scanner(System.in); 
      
      int count = scnr.nextInt();
      int nums[] = new int[count];

// populate array
      for (int i = 0; i < count; ++i) {
         nums[i] = scnr.nextInt();
      }
      
// Step 1: Find and output minimum and maximum values
      int min  = nums[0];
      int max  = nums[0];
      double sum = 0.0;

      for (int i = 0; i < count; ++i) {
         if (nums[i] < min){
            min = nums[i];
         }
         if (nums[i] > max){
            max = nums[i];
         }
         sum = sum + nums[i];
      }
      System.out.println("Minimum: " + min);      
      System.out.println("Maximum: " + max);
      
  // Step 2: Calculate and output mean
      double mean = sum/count;
      System.out.printf("Mean: %.1f\n", mean);
   
// Step 3: Check if palindrome
      int reversed[] = new int[count];
      for (int i = 0; i < count; ++i){
         reversed[count -1 - i] = nums[i];
      }
      
      boolean palindrome = true;
      for (int i =0; i<count; ++i){
         if (nums[i] != reversed[i]) {
            palindrome = false;
            break;
         }
      }
      if (palindrome) {
         System.out.println("Palindrome: true");
      }
      else {
         System.out.println("Palindrome: false");
      }      
      
// Step 4 : Sort array elements in ascending order
      Arrays.sort(nums);
      double median;
      
      if (count % 2 == 1){
         median = nums[count / 2];
      }
      else {
          median = (nums[count / 2 - 1] + nums[count / 2]) / 2.0;
      }
      System.out.printf("Median: %.1f\n", median);
      
      // Step 5: Find and output mode
      
      int mode = nums[0];
      int maxCount = 1;

      int currentNum = nums[0];
      int currentCount = 1;

// Step 5: count frequencies
      for (int i = 1; i < count; ++i) {
         if (nums[i] == currentNum) {
            currentCount++;
         } else {
            // new number → compare with max
            if (currentCount > maxCount) {
               maxCount = currentCount;
               mode = currentNum;
            }
            // reset for next number
            currentNum = nums[i];
            currentCount = 1;
         }
      }

      // check the last run
      if (currentCount > maxCount) {
         mode = currentNum;
      }

      System.out.println("Mode: " + mode); 
   }
}
