/* (zybooks 6.26 LAB: Swapping variables)
Define a method named swapValues that takes an array of four integers as a parameter, 
swaps array elements at indices 0 and 1, and swaps array elements at indices 2 and 3. 
The main program should call function swapValues() to swap array's values and print 
the swapped values on a single line separated with spaces.
Ex: If the input is:
3 8 2 4
function swapValues() returns and the main program outputs:
8 3 4 2  */

import java.util.Scanner;
public class LabProgram {
    public static void swapValues(int[] arrVals) {
      
      for (int i = 0; i < arrVals.length - 1; i += 2) {
      int temp = arrVals[i];
      arrVals[i] = arrVals[i + 1];
      arrVals[i + 1] = temp;
      }
   }

   public static void main(String[] args) {
      Scanner scnr = new Scanner(System.in);            
      int[] userVals = new int[4];  // User values
      int i;                                 // Loop index

// Prompt user to populate array
      for (i = 0; i < userVals.length; ++i) {
         userVals[i] = scnr.nextInt();
      }

// Call method to reverse array values
    swapValues(userVals);
    
     for (i = 0; i < userVals.length; ++i) {
         if (i > 0) {
            System.out.print(" ");  // Print space before elements except the first
      }
      System.out.print(userVals[i]);
   }
   System.out.println();
   }
}
